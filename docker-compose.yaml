services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      FRONTEND_URL: http://localhost:80

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend